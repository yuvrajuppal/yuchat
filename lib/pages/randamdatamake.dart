import 'package:flutter/material.dart';

class randomdatamaker extends StatefulWidget {
  const randomdatamaker({super.key});

  @override
  State<randomdatamaker> createState() => _randomdatamakerState();
}

class _randomdatamakerState extends State<randomdatamaker> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.white,
      body: Column(
        children: [
          
        ],
      ),
    );
  }
}